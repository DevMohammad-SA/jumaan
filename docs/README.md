# توثيق مشروع جُمان — الفهرس

توثيق تقني تفصيلي لمنصة **ملتقى جُمان للفتيات**، ملتقى تعليمي تابع للجمعية
موجَّه لطالبات المرحلتين المتوسطة والثانوية. كُتب بالعربية لأن فريق الصيانة
يتواصل بالعربية.

كل ملف هنا مبني على **فحص الكود الفعلي** في المستودع، لا على افتراضات. عند أي
تعارض، **الكود هو المرجع**. المراجع لمواقع الكود في كل الملفات بصيغة "اسم
الملف + اسم الدالة/الكلاس" بدل رقم السطر — أرقام الأسطر تتغيّر مع كل تعديل
بينما الأسماء لا تتغيّر إلا عمدًا.

## الملفات

| الملف | المحتوى |
|-------|---------|
| [`architecture.md`](architecture.md) | نظرة معمارية: التطبيقان (`accounts` / `participants`)، اتجاه الاعتماد، فلسفة الفصل بين الهوية (`User`) والبيانات البرنامجية (`Participant`)، طبقة العرض، تحويل الحضور لنقاط، **إعداد النشر الإنتاجي عبر Docker**. |
| [`roles-and-permissions.md`](roles-and-permissions.md) | الأدوار الأربعة، كيف يدخل كل دور، ومصفوفة كاملة لصلاحيات كل View مستخرجة من `test_func()`. |
| [`points-system.md`](points-system.md) | العملات الثلاث (نقاط/أميال/نقاط شرائية)، معادلة `apply_points_delta`، القيَم الرقمية الفعلية لكل مصدر نقاط (اللقاء، الحلقة، فعالية الأسبوع، المهمة، النقاط الإضافية)، وسجل النقاط (`PointsLedgerEntry`). |
| [`models.md`](models.md) | كل الموديلات حقلًا حقلًا، القيود (`UniqueConstraint`)، العلاقات (`related_name`)، وما هو مسجَّل في الأدمن. |
| [`authentication.md`](authentication.md) | `NationalIDOrUsernameBackend`، كلمة مرور المشاركة (`must_set_password` + `ForcePasswordSetupMiddleware`)، واسترجاع كلمة المرور بموافقة المشرفة العامة. |
| [`features.md`](features.md) | كل ميزة وظيفية مع الـ View والقالب المسؤولين (بما فيها فعالية الأسبوع، سجل النقاط، النقاط الإضافية، إضافة مشاركة مفردة، التسليم المميز، تعدد صيغ المهام)، وجدول المسارات الكامل. |
| [`known-limitations.md`](known-limitations.md) | **اقرأه أولًا.** القيود التي لا تزال قائمة، **وقسم منفصل يوثّق صراحة كل قيد سابق تغيّر أو حُلّ منذ آخر مراجعة** (مثل بناء إعداد Docker/PostgreSQL، ووجود ملف الترويسة الآن، وتحوّل `Group.supervisor` إلى علاقة متعدد-لمتعدد). |

## نقاط دخول سريعة

- **أريد فهم البنية العامة:** [`architecture.md`](architecture.md).
- **من يستطيع فعل ماذا:** [`roles-and-permissions.md`](roles-and-permissions.md).
- **كيف تُحسب النقاط:** [`points-system.md`](points-system.md).
- **ما الذي لا يجب أن أفترض وجوده، وما الذي تغيّر مؤخرًا:**
  [`known-limitations.md`](known-limitations.md).

## للتشغيل المحلي والنشر الإنتاجي

راجع [`README.md`](../README.md) و[`README.ar.md`](../README.ar.md) في جذر
المشروع، و[`CHANGELOG.md`](../CHANGELOG.md) لسجل الإصدارات.
